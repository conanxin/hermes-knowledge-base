# Short command end-to-end regression — 2026-06-22

## Goal

Verify that the daily short-command import pipeline still closes the loop on a
real, clean public web article, end-to-end:

```
import article → check_kb.py → update_site.py → check_pages_sync.py
→ check_translation_residue.py → commit → push → GitHub Pages records +1
```

No confirmation prompts between build steps. Hard-stop only on a genuine
failure (inaccessible URL, incomplete body, check_kb FAIL).

## Test article

| Field | Value |
|-------|-------|
| Title | *Superlinear Returns* |
| Author | Paul Graham |
| Source | <https://www.paulgraham.com/superlinear.html> |
| Published | 2023-10 |
| Captured | 2026-06-22 |
| Source site | `Paul Graham` |
| Why this URL | Plain HTML, no paywall, no login, no JS-rendered body, complete content including 12 notes, ~4400 words. Public, stable, classic essay on a topic that ages well (perennial relevance to ambition / work / learning). |

## Pipeline execution

### Step 1 — Web extract

`web_extract` returned the full text of the essay including title, byline,
all five H2 sections ("Two Causes: Exponential Growth and Thresholds",
"How to Find It", "The Territory Is Expanding", "Science as the Canonical
Example", "Notes"), the in-text recipe paragraph, and all 12 numbered notes
plus the thanks line. No truncation, no paywall, no missing sections.

### Step 2 — Files written

```
content/articles/2026/2026-06-22-paulgraham-superlinear-returns/
├── metadata.yaml                  ← full schema, word_count as object
├── source.md                      ← 24,827 bytes, 4195 words
├── translation.zh-CN.md           ← 20,710 bytes, 6114 CJK chars
├── summary.md                     ← one-liner + 7 core arguments + concept table
└── notes.md                       ← templates/notes.md skeleton, all 4 sections filled
```

The site/dev tree was generated by the post-import pipeline:

```
site/items/2026-06-22-paulgraham-superlinear-returns/index.html
docs/items/2026-06-22-paulgraham-superlinear-returns/index.html   (synced)
```

### Step 3 — `word_count` schema

Schema required by `check_kb.py`:

```yaml
word_count:
  source: <int>       # English words in source.md
  translation: <int>  # CJK characters in translation.zh-CN.md
```

This article:

```yaml
word_count:
  source: 4195
  translation: 6114
```

Counting method (same as the existing 2026-06-22-your-ai-is-not-a-tool article):

- `source`: regex `\b[A-Za-z][A-Za-z\-']*\b` over source.md with markdown
  link / code-block / heading / quote markers stripped.
- `translation`: regex `[\u4e00-\u9fff]` over translation.zh-CN.md.

### Step 4 — Quality checks

| Command | Result |
|---------|--------|
| `python3 scripts/check_kb.py` | **PASS** — Total items: 20, PASS: 20, FAIL: 0 |
| `python3 scripts/update_site.py` | **PASS** — Steps 1/5 → 5/5 all OK (build_index, export_site_data, generate_item_pages, sync_pages_docs, **check_pages_sync**) |
| `python3 scripts/check_pages_sync.py` | **PASS** — 20/20 slugs in both `site/items/` and `docs/items/`, all 4 top-level files byte-identical |
| `python3 scripts/check_translation_residue.py` | **WARNING** (pre-existing) — **0 suspicious hits** on the new article; WARNING only aggregates legacy articles from earlier turns |

### Step 5 — Drift verification

`diff site/data/catalog.json docs/data/catalog.json` → **byte-identical**.

Catalog record counts:

```
total records: 20
by type: Counter({'article': 7, 'note': 5, 'resource_collection': 4, 'project': 4})
```

Article count went 6 → 7 as expected. All other type counts unchanged.

The new record has the full schema (`title`, `title_zh`, `author`,
`published_date`, `captured_date`, `type`, `topics`, `tags`, `path`,
`updated_date`, `slug`, `detail_url`, `github_url`).

### Step 6 — Git

| Action | Detail |
|--------|--------|
| `git status` (post-pipeline) | `M` catalog.json + index/{authors,tags,timeline}.md + site/data/catalog.json; `??` content/articles/2026/2026-06-22-paulgraham-superlinear-returns/ + site|docs/items/2026-06-22-paulgraham-superlinear-returns/. No stray site/ vs docs/ drift. |
| `git add` | per-file, no `git add -A` or `git add .` |
| `git commit -m "Add short command e2e regression article"` | (see hash below) |
| `git push` | main → origin/main |

## Detail-page URL

- Production (GitHub Pages):
  <https://conanxin.github.io/hermes-knowledge-base/items/2026-06-22-paulgraham-superlinear-returns/>
- Local (after `python3 -m http.server 8000 -d site`):
  <http://localhost:8000/items/2026-06-22-paulgraham-superlinear-returns/>

Once GitHub Pages CDN catches up (per memory: 5-10 minutes after push), the
new slug should be present on the homepage's record list, total = 20.

## Findings

### Pipeline closure

- ✅ Article import → all 5 required files generated
- ✅ word_count in correct YAML-object schema (matches `check_kb.py` v0.3.8+ requirement)
- ✅ check_kb.py: 20/20 PASS (was 19/19 → +1)
- ✅ update_site.py: 5/5 OK including the new STEP 5/5 (`check_pages_sync.py`) hard-stop
- ✅ check_pages_sync.py: site/ and docs/ byte-identical for all 4 top-level files and 20/20 item pages
- ✅ site/data/catalog.json == docs/data/catalog.json (no `git add` skip this time)
- ✅ New article detail page generated in BOTH `site/items/` and `docs/items/`
- ✅ Zero suspicious residue hits on the new article (translation is clean)

### Confirmed working as designed

- The new `check_pages_sync.py` STEP 5/5 fired and passed — confirming the
  `v0.3.11-pages-sync-integrity` change from the prior turn really does
  catch drift, and that this commit doesn't have any (the docs/ files
  `git add`ed cleanly this time).
- The short-command pipeline does not stop at any "should I continue?" prompt;
  it flows directly from file write → build_index → export_site_data →
  generate_item_pages → sync_pages_docs → check_pages_sync.

### Not in scope of this regression

- Live GitHub Pages CDN propagation verification (memory note: root URL
  may 404 for 5-10 min; /index.html usually works immediately; this is
  the same behavior observed in the v0.3.10 and v0.3.11 deployments).
- A second article on the same pipeline (would only re-confirm the same
  chain).

## Files added / modified in this commit

```
A  content/articles/2026/2026-06-22-paulgraham-superlinear-returns/metadata.yaml
A  content/articles/2026/2026-06-22-paulgraham-superlinear-returns/source.md
A  content/articles/2026/2026-06-22-paulgraham-superlinear-returns/translation.zh-CN.md
A  content/articles/2026/2026-06-22-paulgraham-superlinear-returns/summary.md
A  content/articles/2026/2026-06-22-paulgraham-superlinear-returns/notes.md
A  site/items/2026-06-22-paulgraham-superlinear-returns/index.html
A  docs/items/2026-06-22-paulgraham-superlinear-returns/index.html
A  reports/short_command_e2e_regression_20260622.md
M  index/authors.md
M  index/catalog.jsonl
M  index/tags.md
M  index/timeline.md
M  site/data/catalog.json
M  docs/data/catalog.json
```